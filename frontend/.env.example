# Environment Configuration Example
# Copy this file to .env.local for local development overrides

# API Base URL
# - Development (.env.development): http://localhost (gateway port 80)
# - Production (.env.production): Empty (relative paths through gateway)
# - Base (.env): Empty (default for all environments)
VITE_API_BASE_URL=
