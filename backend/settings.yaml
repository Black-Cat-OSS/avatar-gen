app:
  save_path: "./storage/avatars"
  server:
    host: "0.0.0.0"
    port: 3000
  database:
    driver: "sqlite"
    connection:
      maxRetries: 3
      retryDelay: 2000
    sqlite_params:
      url: "file:./storage/database/database.sqlite"
  # Директории автоматически создаются модулем инициализации
  # Не изменяйте эти пути, они синхронизированы с InitializationModule
    # postgresql_params:
    #   host: "localhost"
    #   port: 5432
    #   database: "avatar_gen"
    #   username: "postgres"
    #   password: "password"
    #   ssl: false

